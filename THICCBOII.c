#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

typedef struct BIG{
	unsigned long long q, w, e, r, t, y, u, i, o, p, a, s, d, f, g, h, j, k, l, z, x, c, v, b, n, m;
} THICC_BOII;

void main(){

while(1){
	malloc(sizeof(THICC_BOII)); 
}

}