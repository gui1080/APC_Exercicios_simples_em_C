#include <stdio.h>
#include <math.h> 
 int main()
 {
	double x;

	double a, b, sqrt(double x), cbrt(double x);

	scanf("%lf", &x);

	a=x*x;
	b=x*x*x;

	printf("%.1lf %.1lf %.1lf %.1lf\n", a, b, sqrt(x), cbrt(x));

	return 0;

}
