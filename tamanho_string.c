
#include <stdio.h>
#include <string.h>

int main () {
   int tamanho;
   char stringqualquer [80];
   scanf("%s", stringqualquer);
   tamanho=strlen(stringqualquer);
   printf("%d", tamanho);

   return (0);
}
